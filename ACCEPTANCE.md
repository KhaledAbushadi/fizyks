<div dir="rtl">

# معايير القبول (القسم 8): الحالة بتاريخ 2026-09-25

| # | المعيار | الحالة | الدليل |
|:--|:--|:--|:--|
| 1 | `validate-content` و`npm test` و`npx playwright test` تنجح كلها | **ناجح** | المحتوى: 8 دروس، 35 قالباً (كل قالب 200 نسخة بلا فشل)، 33 مفهوماً خاطئاً، 26 بطاقة. Vitest: 35/35. Playwright: 16/16 على 390×844 و360×740 |
| 2 | تُفتح على أندرويد متوسط عبر رابط Pages، تُثبَّت، وتعمل دون إنترنت | **ناجح جزئياً (يحتاج بشراً)** | اختبار `offline.spec.ts`: بعد التحميل الأول + `setOffline(true)` + إعادة التحميل، يعمل التطبيق والدرس والمعمل. المانيفست والأيقونات 192/512 موجودة. **مفتوح:** النشر الفعلي يحتاج تسجيل دخول خالد (Cloudflare) أو تفعيل Pages، ثم تجربة على موبايل حقيقي |
| 3 | لا تمرير أفقي على 360px في أي شاشة | **ناجح** | `layout.spec.ts` يمر على 13 شاشة ويتحقق أن `scrollWidth ≤ clientWidth` |
| 4 | المعادلات من اليسار لليمين داخل النص العربي دون انقلاب | **ناجح** | `layout.spec.ts`: اتجاه `.math` = ltr وترتيب `I` قبل `=` بصرياً. صفحة `#/dev/content-preview` تعرض القوالب الـ35 بثلاث نسخ بلا أي `katex-error`. المتحقق يرسم كل معادلة بـKaTeX في وضع `throwOnError`. التعبيرات اللاتينية داخل الجمل (مثل R + r) تُعزل تلقائياً بـ`<bdi>` |
| 5 | سلّم التلميحات لا يُتخطى، والمسألة التوأم إلزامية | **ناجح** | `workshop.spec.ts`: 4 إجابات خاطئة ← التلميحات 1 ثم 2 ثم 3 بالترتيب ← الحل ← مسألة توأم بأرقام مختلفة، وصفر أزرار تخطٍّ |
| 6 | كل بديل خاطئ في الفصل 1 مربوط بمفهوم خاطئ له شرح | **ناجح** | المتحقق يرفض أي `misconception` غير موجود في القاموس (مختبر بملف معطوب عمداً). البدائل التكميلية مربوطة بـ`M-POWER-OF-TEN` |
| 7 | نفس المهارة لا تعرض نفس الأرقام مرتين في 10 محاولات متتالية | **ناجح** | `freshInstance` يستبعد بصمات آخر 10 محاولات للمهارة (من البذور المحفوظة). المتحقق يشترط 11 نسخة مختلفة على الأقل لكل قالب |
| 8 | صفر طلبات شبكة لنطاقات خارجية؛ زر المساعد ينسخ فقط | **ناجح** | `trackExternal` في اختبارات الورشة والدرس والعرض = صفر طلبات. اختبار الزر يسجل كل الطلبات أثناء الضغط = صفر، والحافظة فيها نص المسألة بأرقامها و"لا تعطني الحل". محاكاة PhET لا تُحمَّل إلا بلمسة صريحة |
| 8.1 | صفر مدفوعات | **ناجح** | البحث بالكلمات الأربع المحددة في القسم 8.1 داخل المستودع (خارج ملف الخطة و`node_modules`) = صفر نتائج. لا خادم، لا مفاتيح، لا خدمات. التراخيص (`license-checker --production`): MIT ×16، Apache-2.0 ×4، والبند UNLICENSED الوحيد هو مشروع فُكّها نفسه (خاص). أدوات التطوير: MIT/ISC/BSD/Apache/BlueOak/MPL-2.0/CC-BY-4.0 (بيانات caniuse)، كلها مفتوحة المصدر، ولا ترخيص تجاري. الخط IBM Plex Sans Arabic بترخيص OFL-1.1 مضمَّن محلياً |
| 9 | مراجعة المحتوى وتحويله إلى `reviewed`، ومطابقة الثوابت | **مفتوح (يحتاج بشراً)** | كل ملفات الفصل 1 والصالة `draft`. نصوص "زي ما في الكتاب" منقولة من كتاب الوزارة المرفق، والأمثلة مطابقة لأمثلته (مثلاً كيرشوف: I₃ = 0.71 وI₂ = −0.516، مختبرة). `constants.json`: `verified: false` مع `bookValue` من ملحق 2 (ثابت بلانك: حُسم باعتماد قيمة الكتاب 6.63×10⁻³⁴؛ باقي الثوابت تنتظر المطابقة) |
| 10 | اختبار ميداني 7 أيام، ومؤشر نجاح بعد 4 أسابيع | **مفتوح (يحتاج بشراً)** | الأدوات جاهزة: الاختبار القبلي عند أول تشغيل، والبعدي يُفتح بعد إتقان كل مهارات الفصل ويعرض المقارنة. نسبة الصحيح من أول محاولة في تذكرة الخروج محفوظة في `lessonProgress.exitScore`، وكل محاولة في `attempts` |

## بنود مفتوحة تحتاج خالد أو مدرس فيزياء

1. **مراجعة المحتوى** (4–6 ساعات تقريباً): افتح "المزيد ← معاينة المحتوى" بعد لمس الشعار 5 مرات، أو `npm run dev`.
2. **الثوابت**: طابق `content/constants.json` مع الكتاب وحوّل `verified` إلى `true`.
3. **النشر**: Cloudflare Pages (`npx wrangler login` مرة واحدة) أو GitHub Pages (Settings ← Pages ← GitHub Actions) — راجع README.
4. **محاكاة PhET محلياً**: التنزيل رُفض من بيئة البناء (403)؛ الأمر موجود في README لتشغيله من أي جهاز فيه نت.
5. **الاختبار الميداني** مع الطالب.

</div>
